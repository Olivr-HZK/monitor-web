#
# 复制为 `.env.development`
#

# —— 连本机后端（默认）——
# VITE_DEV_API_PROXY=http://127.0.0.1:3001

# —— 连线上后端（本地前端联调）——
# 不要写直连的 API 基地址；用下面代理 + 重启 npm run dev（vite 开发模式会强制清空 VITE_API_BASE_URL）
VITE_API_BASE_URL=
VITE_API_BASE=
VITE_DEV_API_PROXY=https://api.gurublog.uk
