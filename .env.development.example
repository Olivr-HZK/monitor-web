#
# 复制为 `.env.development`
#

# —— 连本机后端（默认）——
# VITE_DEV_API_PROXY=http://127.0.0.1:3001

# —— 连线上后端（本地前端联调）——
# 不要写 VITE_API_BASE_URL；用下面一行 + 重启 npm run dev
VITE_DEV_API_PROXY=https://api.gurublog.uk
