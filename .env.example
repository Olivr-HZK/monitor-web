# 复制为 .env 并填写实际值
# OpenRouter API Key，供 scripts/generate_top5_insight.py 生成 Top5 异动陈述（Kimi 2.5）
# 获取地址：https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# 周报推送（可与 backend/.env 共用变量名）；飞书 ST 列式卡片左侧图标另需 FEISHU_APP_ID + FEISHU_APP_SECRET（上传图片换 image_key）
# FEISHU_WEBHOOK_URL=
# WECOM_WEBHOOK_URL=
# FEISHU_APP_ID=
# FEISHU_APP_SECRET=

# 前后端分离部署时，构建前端用的后端地址（可选，也可在构建命令里写）
# 生产默认见根目录 `.env.production`（当前为 https://api.gurublog.uk）
# 本地可建 `.env.development` 覆盖为 http://localhost:3001
# VITE_API_BASE_URL=
