# 复制为 .env.production（该文件已被 .gitignore，勿提交密钥）
# 静态站（GitHub Pages）+ 可选公网 API

# --- 仅静态鉴权 + 静态 .db（已不推荐；仅应急）---
# 设为 1 时：登录为「访问密码」、数据从同站点 public/*.db 加载，不走后端 /api/me、/api/data。
# 当前生产默认走 API 模式：/api/data/*.db 由后端映射到上游项目源库并返回请求级 SQLite 快照。
# 仍可保留下方 VITE_API_BASE_URL，供站内 AI 等调用独立 API（与鉴权/数据文件解耦）。
# VITE_FORCE_STATIC_AUTH=1

# --- 前后端分离：账号密码 + /api/data（需后端 CORS + SameSite=none，见 backend/.env.example）---
VITE_API_BASE_URL=https://api.gurublog.uk
