# monitor-web 统一定时调度

本文说明当前整合方式：把监测链核心入口收口到 `monitor-web/scripts/cron/run_job.sh` 与 `pipelines/monitor-chain/`，并把四份核心数据库统一写入或镜像到 `monitor-web/data/databases/`。统一入口负责补齐 PATH、加锁、记录日志和输出最后状态。

## 目录与文件

| 文件 | 作用 |
|------|------|
| `scripts/cron/run_job.sh` | 统一 job runner，按 job id 调用外部项目或本仓库脚本 |
| `scripts/cron/monitor-web-unified.cron.fragment.txt` | 统一 crontab 片段模板 |
| `scripts/cron/install_unified_cron.sh` | 预览/安装统一 crontab，可移除已知旧 cron 行 |
| `scripts/db/mirror_databases.py` | 用 SQLite backup API 把上游四库镜像到 `data/databases/` |
| `data/databases/*.db` | monitor-web 内 canonical 数据库快照（被 git 忽略） |
| `logs/jobs/<job-id>.log` | 每个 job 的统一日志 |
| `logs/jobs/<job-id>.last_status` | 每个 job 最近一次状态 |
| `logs/jobs/locks/<job-id>.lock` | 防止同一 job 重叠运行的锁目录 |

## 当前纳入的 job

### monitor-web 监测链

| job id | 原项目/动作 |
|--------|-------------|
| `competitor_daily_scraper` | `monitor-web/pipelines/monitor-chain/competitor-social/run-daily-scraper.sh` |
| `competitor_weekly_period` | `monitor-web/pipelines/monitor-chain/competitor-social/run-weekly-period-workflow.sh` |
| `wechat_douyin_weekly` | `monitor-web/pipelines/monitor-chain/wechat-douyin/scripts/weekly_wx_three_charts_scrape_and_import.sh` |
| `wechat_douyin_weekly_rerun` | `monitor-web/pipelines/monitor-chain/wechat-douyin/scripts/rerun_weekly_wx_three_charts_if_needed.sh` |
| `sensortower_weekly` | `monitor-web/pipelines/monitor-chain/sensortower/scripts/cron_run_weekly.sh`，默认 `SKIP_SENSORTOWER_WEEKLY_PUSH=1` |
| `sensortower_us_free_daily` | `monitor-web/pipelines/monitor-chain/sensortower/scripts/cron_run_us_free_daily.sh` |
| `sensortower_arrow_madness_daily` | `monitor-web/pipelines/monitor-chain/sensortower/scripts/cron_run_arrow_madness_daily.sh` |
| `monitor_chain_check` | `monitor-web/scripts/check_monitor_chain.sh` |
| `monitor_chain_checked_push` | `monitor-web/scripts/sync_dbs_and_deploy.sh`，默认 `SYNC_SKIP_DEPLOY=1`，后端与 `/api/data` 检查通过后才推送 |
| `monitor_reports_daily` | `monitor-web/scripts/run_reports.sh --content hot,ai`，暂未默认排入 crontab |

### 其它本机定时项目

| job id | 原项目/动作 |
|--------|-------------|
| `xiaohei_ainews_prepare` / `xiaohei_ainews_push` | xiaohei-agent AI news prepare / push |
| `trendradar_ainews_prepare` / `trendradar_ainews_push` | TrendRadar AInews Top20 |
| `trendradar_daily_0800` / `trendradar_daily_0900` | TrendRadar daily workflow |
| `trustmrr_daily` / `trustmrr_weekly_generate` / `trustmrr_weekly_push` | trustmrr 飞书推送 |
| `aitools_cleanup_logs` / `aitools_backup_db` / `aitools_daily_scraper` / `aitools_weekly_period` | AITools Competitor Monitor |
| `gaming_daily` / `gaming_weekly_generate` / `gaming_weekly_push` | gaming-daily-report2 |
| `ai_video_enhancer_daily` / `ai_ve_feedback_training_daily` / `ai_arrow2_latest_daily` / `ai_arrow2_exposure_wed_sat` | AI UA workflows |
| `festivals_daily_bitable` / `festivals_weekly_push` | festivals-marketing |
| `task_butler_watch_send` | task butler project reports |

## 使用方式

查看所有可用 job：

```bash
./scripts/cron/run_job.sh --help
```

预览某个 job 会执行什么，不真正运行：

```bash
JOB_DRY_RUN=1 ./scripts/cron/run_job.sh sensortower_weekly
```

预览统一 crontab 完整内容，不写入系统：

```bash
./scripts/cron/install_unified_cron.sh
```

只打印统一 block：

```bash
./scripts/cron/install_unified_cron.sh --print-block
```

安装统一 block，并移除当前 crontab 中已知的旧入口，避免重复运行：

```bash
./scripts/cron/install_unified_cron.sh --apply --replace-known
```

安装脚本会先备份当前 crontab 到：

```text
logs/cron-backups/crontab-before-unified-YYYYMMDD-HHMMSS.txt
```

并保存候选 crontab 到：

```text
logs/cron-backups/crontab-unified-next-YYYYMMDD-HHMMSS.txt
```

如果 macOS `crontab` 写入卡住，安装脚本会按 `CRONTAB_INSTALL_TIMEOUT`（默认 20 秒）超时退出，不会留下悬挂的 `crontab` 进程。此时先保留旧 cron wrapper 转发；SensorTower 和竞品社媒的旧 wrapper 已进入 `scripts/cron/run_job.sh`，待系统 crontab 恢复后再安装候选文件。

当前已确认的系统层卡点：

```text
launchctl kickstart -k system/com.vix.cron
=> Operation not permitted while System Integrity Protection is engaged
```

也就是说，重启系统 cron daemon 需要本机人工授权或重启。人工介入后执行：

```bash
cd /Users/ggbond/lyb/monitor-web
./scripts/cron/install_unified_cron.sh --apply --replace-known
```

若仍卡住，可先重启电脑，再执行上面的安装命令；候选 crontab 已保存在 `logs/cron-backups/crontab-unified-next-*.txt`。

## 路径约定

默认按当前机器路径推断：

| 变量 | 默认值 |
|------|--------|
| `LYB_ROOT` | `monitor-web` 的上一级，也就是 `/Users/ggbond/lyb` |
| `WORKSPACE_ROOT` | `$HOME/workspace` |
| `OLIVER_ROOT` | `$HOME/oliver` |
| `OPENCLAW_WORKSPACE` | `$HOME/.openclaw/workspace` |
| `MONITOR_WEB_JOB_LOG_DIR` | `monitor-web/logs/jobs` |
| `MONITOR_LOCAL_DB_DIR` | `monitor-web/data/databases` |

换机器时可在 crontab 或 shell 环境里覆盖这些变量。

## 第一阶段边界

- 微信/抖音、SensorTower、竞品社媒核心脚本已迁入本项目，其余外部项目源码继续按产品域逐个迁移。
- 不合并各项目依赖环境。
- 数据库已镜像到 `monitor-web/data/databases/`，但该目录被 git 忽略，不纳入版本管理。
- 微信/抖音三榜业务逻辑已迁入 `pipelines/monitor-chain/wechat-douyin/`；SensorTower 三条核心入口已迁入 `pipelines/monitor-chain/sensortower/`；竞品社媒两条核心入口已迁入 `pipelines/monitor-chain/competitor-social/`。

后续需要做的是清理旧仓库转发 wrapper、给 Python pipeline 建独立 `.venv`，并在稳定运行一周后移除后端旧路径 fallback。
